# Same Supabase project as the web app (web uses VITE_* names for these values).
# EXPO_PUBLIC_* vars are inlined into the JS bundle — anon key only, never service_role.
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
